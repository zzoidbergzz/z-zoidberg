# 2026-05-07

- Brought up `security-knowledge` locally on `http://127.0.0.1:8010` as transient systemd unit `security-knowledge-8010.service`.
- Started project Postgres via `sudo docker compose up -d db`; Redis was already available on `127.0.0.1:6379`.
- Applied Alembic migrations through `0012` after fixing the unused/invalid `TIMESTAMPTZ` import in `alembic/versions/0012_mitre_cache.py`.
- Seeded default auth data. `m@z.je` exists as an approved admin and one active `ADMIN-KEY` exists. One-time admin key output is stored at `security-knowledge/.runtime/seed-admin-2026-05-07.log` with mode `0600`; do not copy key into chat/memory.
- Fixed local startup/UI compatibility issues: structlog factory in `app/observability/logging.py`, root redirect in `app/main.py`, and current Starlette `TemplateResponse` call style in `app/ui/routes.py`.
- Did not repoint public `https://z.je/`; current Apache vhost serves `/var/www/zje/public_html` and no Apache auth file was present.
